auto-install-peers=true
strict-peer-dependencies=false

# apps/web has a prebuild step that copies legacy/v1 into public/ so the
# original app stays served at /v1. pnpm does not run pre/post scripts by
# default, and silently skipping it would drop that route.
enable-pre-post-scripts=true
